EXEC = prog_client.elf

RM = rm -f

CC = gcc

CCFLAGS = -Wall -MMD -MP
LDFLAGS = -lm

SRC = client.c

OBJ = $(SRC:.c=.o)
DEP = $(OBJ:.o=.d)


all: $(EXEC)

$(EXEC): $(OBJ)
	$(CC) $^ -o $@ $(LDFLAGS)

-include $(DEP)

%.o: %.c
	$(CC) $(CCFLAGS) -c $< -o $@

clean:
	$(RM) $(OBJ) $(DEP) $(EXEC)